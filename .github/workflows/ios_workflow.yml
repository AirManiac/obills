name: Build Unsigned Debug iOS IPA

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-ios-debug:
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build Next.js Static Export
        run: npm run build

      - name: Sync Capacitor iOS
        run: |
          rm -rf ios
          npx cap add ios
          npx cap sync ios

      - name: Build Unsigned Debug Xcode Archive
        run: |
          xcodebuild archive \
            -project ios/App/App.xcodeproj \
            -scheme App \
            -configuration Debug \
            -sdk iphoneos \
            -archivePath build/App-Debug.xcarchive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            PROVISIONING_PROFILE_SPECIFIER=""

      - name: Package Unsigned Debug .ipa File
        run: |
          mkdir -p build/Payload
          cp -R build/App-Debug.xcarchive/Products/Applications/App.app build/Payload/
          cd build
          zip -r App-Debug-unsigned.ipa Payload

      - name: Upload Debug IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: unsigned-debug-ios-ipa
          path: build/App-Debug-unsigned.ipa
          retention-days: 7